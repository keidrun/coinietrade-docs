{
  "name": "coinietrade-docs",
  "version": "0.1.0",
  "description": "Keid's CoinieTrade project - Documents.",
  "repository": "https://github.com/keidrun/coinietrade-docs.git",
  "author": "Keid",
  "license": "No license",
  "private": true,
  "scripts": {
    "dev": "gitbook serve --port 8080",
    "build": "gitbook build",
    "build:debug": "gitbook build ./ --log=debug --debug",
    "deploy": "git subtree push --prefix _book origin gh-pages",
    "deploy:force": "git push origin `git subtree split --prefix _book master`:gh-pages --force"
  },
  "devDependencies": {
    "gitbook-cli": "^2.3.2"
  }
}
